name: Generate Snake

on:
  schedule:
    # Toutes les 12 h
    - cron: "0 */12 * * *"
  workflow_dispatch: # bouton "Run workflow" manuel
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Générer le SVG du snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark
            dist/github-snake-pink.svg?color_snake=%23FF6FB5&color_dots=%23170c13,%234a0f2e,%238a1a55,%23d1206f,%23FF1F8F

      - name: Pousser vers la branche "output"
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
